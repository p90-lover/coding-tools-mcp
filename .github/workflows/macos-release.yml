name: Retired macOS manual release

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain the verified release path
        run: >-
          echo "This legacy workflow is retired and globally disabled.
          Stable Windows and universal macOS assets are produced only by
          .github/workflows/release.yml after its complete verification gates."
