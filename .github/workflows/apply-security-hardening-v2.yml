name: Apply security hardening v2

on:
  push:
    branches:
      - security/unauthorized-auth-trash-heavy-usage
    paths:
      - "aiTemp/security-hardening/apply_security_hardening_v2.py"
      - "aiTemp/security-hardening/fix_apply_security_hardening_v2_toml.py"
      - "aiTemp/security-hardening/fix_apply_security_hardening_v2_oauth_fixture.py"
      - "aiTemp/security-hardening/fix_apply_security_hardening_v2_rust_generation.py"
      - "aiTemp/security-hardening/fix_apply_security_hardening_v2_rust_generation_v2.py"
      - "aiTemp/security-hardening/fix_apply_security_hardening_v2_linked_roots.py"
      - "aiTemp/security-hardening/apply_linked_root_transaction_fix.py"
      - "aiTemp/security-hardening/apply_public_tunnel_auth_tests.py"
      - "aiTemp/security-hardening/apply_public_tunnel_auth_lifecycle_tests.py"
      - "aiTemp/security-hardening/apply_public_tunnel_auth_hardening.py"
      - "aiTemp/security-hardening/apply_frp_restore_fail_closed.py"
      - "aiTemp/security-hardening/apply_codex_permission_tests.py"
      - "aiTemp/security-hardening/apply_codex_permission_model.py"
      - "aiTemp/security-hardening/test_apply_security_hardening_v2.py"
      - "aiTemp/security-hardening/test_linked_root_transaction_generation.py"
      - ".github/workflows/apply-security-hardening-v2.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-security-hardening-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  CARGO_TARGET_DIR: ${{ github.workspace }}/aiTemp/cargo-target-security-hardening-apply

jobs:
  apply-test-commit:
    runs-on: ubuntu-24.04
    timeout-minutes: 100
    steps:
      - uses: actions/checkout@v4
        with:
          ref: security/unauthorized-auth-trash-heavy-usage
          fetch-depth: 0

      - name: Install Tauri Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libssl-dev \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libxdo-dev

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          shared-key: apply-security-hardening-linux

      - name: Validate hardening applicators parse
        run: |
          python -m py_compile \
            aiTemp/security-hardening/apply_security_hardening_v2.py \
            aiTemp/security-hardening/fix_apply_security_hardening_v2_toml.py \
            aiTemp/security-hardening/fix_apply_security_hardening_v2_oauth_fixture.py \
            aiTemp/security-hardening/fix_apply_security_hardening_v2_rust_generation.py \
            aiTemp/security-hardening/fix_apply_security_hardening_v2_rust_generation_v2.py \
            aiTemp/security-hardening/fix_apply_security_hardening_v2_linked_roots.py \
            aiTemp/security-hardening/apply_linked_root_transaction_fix.py \
            aiTemp/security-hardening/apply_public_tunnel_auth_tests.py \
            aiTemp/security-hardening/apply_public_tunnel_auth_lifecycle_tests.py \
            aiTemp/security-hardening/apply_public_tunnel_auth_hardening.py \
            aiTemp/security-hardening/apply_frp_restore_fail_closed.py \
            aiTemp/security-hardening/apply_codex_permission_tests.py \
            aiTemp/security-hardening/apply_codex_permission_model.py \
            aiTemp/security-hardening/test_apply_security_hardening_v2.py \
            aiTemp/security-hardening/test_linked_root_transaction_generation.py

      - name: Apply reviewed applicator fixes with recoverable backups
        run: |
          python aiTemp/security-hardening/fix_apply_security_hardening_v2_toml.py
          python aiTemp/security-hardening/fix_apply_security_hardening_v2_oauth_fixture.py
          python aiTemp/security-hardening/fix_apply_security_hardening_v2_rust_generation.py
          python aiTemp/security-hardening/apply_linked_root_transaction_fix.py
          python aiTemp/security-hardening/apply_frp_restore_fail_closed.py

      - name: Reproduce applicator dependency and Rust generation
        run: |
          python aiTemp/security-hardening/test_apply_security_hardening_v2.py
          python aiTemp/security-hardening/test_linked_root_transaction_generation.py

      - name: Apply backup-first hardening without deleting files
        shell: bash
        run: |
          python aiTemp/security-hardening/apply_security_hardening_v2.py
          python aiTemp/security-hardening/apply_public_tunnel_auth_tests.py
          python aiTemp/security-hardening/apply_public_tunnel_auth_lifecycle_tests.py
          python aiTemp/security-hardening/apply_public_tunnel_auth_hardening.py
          python aiTemp/security-hardening/apply_codex_permission_tests.py
          python aiTemp/security-hardening/apply_codex_permission_model.py
          cargo check --manifest-path src-tauri/Cargo.toml
          cargo fmt --manifest-path src-tauri/Cargo.toml --all
          git diff --check
          if git diff --name-status | grep -q '^D'; then
            echo "Refusing to continue because hardening deleted a file."
            exit 1
          fi

      - name: Run targeted security tests
        working-directory: src-tauri
        run: |
          cargo test --locked codex_permission_ -- --nocapture
          cargo test --locked public_tunnel_auth_ -- --nocapture
          cargo test --locked empty_configured_client_id_is_rejected -- --nocapture
          cargo test --locked redirect_uri_policy_rejects_untrusted_destinations -- --nocapture
          cargo test --locked move_to_trash_preserves_content -- --nocapture
          cargo test --locked apply_patch_allows_approved_linked_absolute_path_target -- --nocapture

      - name: Run complete Rust tests
        working-directory: src-tauri
        run: cargo test --locked

      - name: Run strict Rust lint
        working-directory: src-tauri
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Run complete frontend and source-contract gates
        run: |
          mkdir -p aiTemp/npm-cache
          npm ci --cache aiTemp/npm-cache
          npm run check
          npm run build
          node --test tests/*.test.mjs

      - name: Scan changed source for credential-shaped literals
        shell: bash
        run: |
          changed_files=$(git diff --name-only -- src-tauri src tests)
          if [ -n "$changed_files" ]; then
            if grep -nE '(gh[pousr]_[A-Za-z0-9_]{20,}|sk-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|BEGIN (RSA|OPENSSH|EC) PRIVATE KEY)' $changed_files; then
              echo "Credential-shaped literal detected in changed source."
              exit 1
            fi
          fi

      - name: Commit only verified materialized source and lock metadata
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- \
            src-tauri \
            src \
            tests \
            package.json \
            package-lock.json
          git add -u -- aiTemp/security-hardening/apply_security_hardening_v2.py
          if git diff --cached --quiet; then
            echo "Security hardening source is already materialized."
            exit 0
          fi
          if git diff --cached --name-status | grep -q '^D'; then
            echo "Refusing to commit a deletion."
            exit 1
          fi
          git commit -m "security: materialize verified hardening source"

          for attempt in 1 2 3; do
            git fetch origin security/unauthorized-auth-trash-heavy-usage
            if git rebase origin/security/unauthorized-auth-trash-heavy-usage && \
               git push origin HEAD:security/unauthorized-auth-trash-heavy-usage; then
              exit 0
            fi
            git rebase --abort || true
            sleep $((attempt * 5))
          done

          echo "Unable to publish verified hardening source after three non-destructive retries."
          exit 1
