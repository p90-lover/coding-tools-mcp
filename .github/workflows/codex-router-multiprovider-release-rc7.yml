name: Coding Tools v0.7.0-rc.7 exact-source Windows release

on:
  push:
    branches:
      - release/codex-router-multiprovider-0.7.0-rc.7
    paths:
      - .github/workflows/codex-router-multiprovider-release-rc7.yml
      - aiTemp/rc7-release/run-windows-release.mjs
      - aiTemp/rc7-release/verify-windows-migration.ps1
      - desktop-electron/**
      - runtime-web/**
      - rust-core/**
      - scripts/release/**
      - docs/releases/v0.7.0-rc.7.md
  workflow_dispatch:

permissions:
  actions: read
  contents: write

concurrency:
  group: coding-tools-v0.7.0-rc.7-${{ github.ref }}
  cancel-in-progress: false

env:
  RELEASE_VERSION: 0.7.0-rc.7
  RELEASE_TAG: v0.7.0-rc.7
  NODE_VERSION: '22'
  BUN_VERSION: 1.4.0
  RUST_TOOLCHAIN: 1.98.1
  PYTHONDONTWRITEBYTECODE: '1'

jobs:
  release-windows:
    name: Verify, package, migrate, and publish Windows x64
    runs-on: windows-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          clean: false
          persist-credentials: false

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}

      - name: Validate release runners
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          node --check aiTemp/rc7-release/run-windows-release.mjs
          if ($LASTEXITCODE -ne 0) { throw 'Node release runner syntax validation failed' }
          $tokens = $null
          $errors = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile(
            (Join-Path $env:GITHUB_WORKSPACE 'aiTemp\rc7-release\verify-windows-migration.ps1'),
            [ref]$tokens,
            [ref]$errors
          )
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.ToString() }
            throw 'PowerShell migration runner syntax validation failed'
          }

      - name: Run exact-source release pipeline
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          node aiTemp/rc7-release/run-windows-release.mjs
          if ($LASTEXITCODE -ne 0) { throw "release runner failed with exit code $LASTEXITCODE" }

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: coding-tools-v0.7.0-rc.7-evidence-${{ github.sha }}
          path: |
            aiTemp/evidence/
            aiTemp/release-assets/
          if-no-files-found: warn
          retention-days: 14
