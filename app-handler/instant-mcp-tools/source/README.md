# Instant MCP Tools (in-tree)

Bundled control surface for the Desktop MCP tool registry. `inspect` / `listTools` / `runTool` are in-process `codingTools.apps` operations. There is no dedicated listen port and no download-to-use step.
