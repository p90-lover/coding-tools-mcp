# Anneal in-tree module

Pinned to mosonlab/anneal `e43b72b`. Package-time extraResources unpack into the Desktop bundle; this folder is the Coding Tools module home. Visual board `127.0.0.1:5173`, API acts `127.0.0.1:3000`. If Postgres / node-gyp / gyp cannot bind `:3000`, handlers soft-fail instead of freezing Coding Tools.
