# Rust port validation

The reviewed Rust source is committed on this branch. This run applies the focused Windows cold-start test allowance in memory, then runs formatting, the complete Rust tests, and Clippy on Linux, Windows, and macOS. The production timeout behavior is unchanged, and no files are deleted.
