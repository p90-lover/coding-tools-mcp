# Rust port validation

This temporary marker opens the same-repository validation pull request. The workflow applies the reviewed Rust patch in each operating-system job, runs the complete Rust test and Clippy suites, then commits the verified Rust source without deleting files.
