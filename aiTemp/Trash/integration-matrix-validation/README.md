# Full integration matrix validation

This temporary marker runs the existing xyTom 0.3 integration workflow against the committed integration branch state. It validates the frontend, Rust compatibility contracts, Linux, Windows, and macOS. It is kept under `aiTemp/Trash/`; no project files are deleted.
